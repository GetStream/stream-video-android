<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="io.getstream.video.chat_with_video.application.ChatWithVideoApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StreamChatWithVideo">
        <activity
            android:name="io.getstream.video.chat_with_video.ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.StreamChatWithVideo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name="io.getstream.video.chat_with_video.ui.channels.ChannelsActivity" />
        <activity
            android:name="io.getstream.video.chat_with_video.ui.messages.MessagesActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name="io.getstream.video.chat_with_video.ui.call.CallActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:supportsPictureInPicture="true" />

        <service
            android:name="io.getstream.video.chat_with_video.ui.call.CallService"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>